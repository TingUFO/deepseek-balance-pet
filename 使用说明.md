# DeepSeek 余额小宠物

一个可以一直挂在屏幕上的可爱小宠物：蓝发女仆娘，头顶的气泡会实时显示你 DeepSeek 账户的余额。
你可以把它拖到任何位置。

## 怎么用

1. 启动：双击 `启动宠物.bat`，宠物会出现在屏幕左上角。
2. 设置 API Key：在宠物上右键，选择“设置 API Key…”，把 DeepSeek 的 API Key 粘进去（形如 `sk-xxxxxxxx`）。
   - 也可以直接编辑 `config.json`，把 `"api_key"` 填上；或者设置系统环境变量 `DEEPSEEK_API_KEY`。
   - Key 只保存在你电脑本地，不会上传。
3. 设置好之后，气泡就会每隔一段时间自动刷新余额。

## 操作

- 拖动宠物本体：移动整个宠物（窗口）到喜欢的位置。
- 拖动气泡：单独调整气泡位置，松开后会自动记住；下次启动仍是这个位置。
- 拖动小圆圈：单独调整圆点位置（和气泡分开摆），松开后自动记住。
- 右键：刷新余额 / 隐藏或显示气泡与圆点 / 调整大小 / 设置 API Key / 锁定位置 / 重置位置 / 退出。
- 隐藏时会把气泡和下面的两个小圆点一起藏起来，只留宠物本体。
- 调整大小：右键 → 调整大小，可选 0.8x / 1.0x / 1.2x / 1.5x / 2.0x，整只宠物、气泡、圆点一起等比缩放并记住。

## 怎么获取 DeepSeek API Key

登录 DeepSeek 开放平台，在“API Keys”页面新建一个 Key，复制 `sk-` 开头的那串就是。

> 注意：余额接口读取的是你有权限看到的账户余额。如果 Key 无效或网络不通，气泡会提示“获取失败 / 网络错误”。

## 自动刷新间隔

默认每 60 秒刷新一次。想改的话，编辑 `config.json` 里的 `"poll_seconds"`（单位：秒）后重新启动。

## 文件说明

- `pet.py` — 宠物主程序
- `pet.png` — 宠物形象（来自高清图）
- `config.json` — 你的 API Key、刷新间隔与气泡位置
- `启动宠物.bat` — 双击即可启动
- `_ref_hi.png` — 高清角色原图
- `make_assets_hires.py` — 从高清图生成宠物形象素材（一般不需要运行）
- `宠物预览.png` — 效果预览图
